---
public: true
title: Recording screen in Linux using ffmpeg
---

Recording screen in [Linux](Linux) using ffmpeg

```sh
# Without sound or mouse
ffmpeg -video_size 1920x1080 -draw_mouse 0 -framerate 60 -f x11grab -i :0.0 -c:v libx264 -crf 16 -preset ultrafast out.mkv -y
```
